import fetch from 'node-fetch' export default async function handler(req, res) { const symbol = req.query.symbol || 'NIFTY' try { const url = `https://www.nseindia.com/api/option-chain-indices?symbol=${encodeURIComponent(symbol)}` const headers = { 'Accept': 'application/json, text/plain, */*', 'Accept-Language': 'en-US,en;q=0.9', 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/117.0.0.0 Safari/537.36' } const r = await fetch(url, { headers }) const text = await r.text() try { const data = JSON.parse(text) return res.status(200).json(data) } catch (err) { return res.status(502).json({ error: 'Failed to parse NSE response', raw: text.slice(0,1000) }) } } catch (e) { return res.status(500).json({ error: e.message }) } }