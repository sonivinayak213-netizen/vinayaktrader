export function analyze({pe, de, adx, superTrend}) { let fund = (pe !== undefined && pe < 20 && de !== undefined && de < 1) let tech = (adx !== undefined && adx > 25 && superTrend === 'UP') if (fund && tech) return 'Fundamentally & technically strong — Consider BUY for swing.' if (fund && !tech) return 'Fundamentally strong but technically weak — Consider HOLD.' if (!fund && tech) return 'Technically strong but fundamentals weak — Trade with caution.' return 'Weak both fundamentally and technically — Avoid / Research more.' }